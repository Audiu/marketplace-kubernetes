grafana:
  adminPassword: changeme
  ingress:
    enabled: true
    hosts: 
      - grafana.repostexchange.com

kubelet:
  namespace: prometheus-operator

prometheus:
  prometheusSpec:
    retention: 21d

prometheusOperator:
  kubeletService:
    namespace: prometheus-operator
